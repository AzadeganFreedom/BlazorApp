@page "/"
@using BlazorApp.Data;

<PageTitle>Index</PageTitle>

<h1>Welcome Back!</h1>

Here is your personal artist database!

<button @onclick="ReadSql">Read From Sql</button>

@if (artistList == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th></th>
                <th>Name</th>
                <th>Genre</th>
                <th>URL</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var artist in artistList)
            {
                <tr>
                    <!--<td>@artist.Id</td>-->
                    <td></td>
                    <td>@artist.Name</td>
                    <td>@artist.Genre</td>
                    <td><a href="@artist.Url" target="-blank">@artist.Url</a></td>
                    <td><button @onclick="() => DeleteFromSql(artist.Id)">Delete</button></td>
                </tr>
            }

            <tr>
                <td><button @onclick="AddToSql" >Add</button></td>
                <td><input @bind = "artist.Name"/></td>
                <td><input @bind = "artist.Genre"/></td>
                <td><input @bind = "artist.Url"/></td>

            </tr>
        </tbody>
    </table>
}

@code {
    Sql sql = new();
    Artist artist = new();
    List<Artist> artistList = Sql.Read();
    void ReadSql() { artistList = Sql.Read(); }
    void AddToSql()
    { 
        Sql.Create(artist); 
        List<Artist> artistList = Sql.Read();
    }
    void DeleteFromSql(int id)
    {
        Sql.Delete(id);
        artistList = Sql.Read();
    }
}